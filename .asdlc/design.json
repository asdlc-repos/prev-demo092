{
  "overview": "A microservices architecture with a React frontend (leave-web-app) and a Go backend API (leave-service) that manages employee leave requests, approvals, and balances. The frontend communicates with the backend via REST API, and the backend stores leave requests, user roles, and balances in memory or a simple data store. Managers can approve/reject requests from their direct reports, while employees can submit and track their own requests.",
  "requirements": [
    "Employees must be able to submit leave requests specifying start date, end date, and leave type",
    "Managers must be able to view pending leave requests from their direct reports only",
    "Managers must be able to approve or reject leave requests",
    "Employees must be able to view the status of their submitted leave requests (pending, approved, rejected)",
    "All users must be able to view their current leave balance by leave type",
    "The system must enforce manager-employee reporting relationships",
    "The system must support multiple leave types (e.g., annual, sick, personal)",
    "The system must provide health check endpoints for all services",
    "The backend API must be documented with OpenAPI specification",
    "The frontend must be a single-page application accessible via web browser"
  ],
  "components": [
    {
      "name": "leave-service",
      "componentType": "service",
      "language": "Go",
      "dependsOn": [],
      "entrypoint": "deployment/service",
      "buildpack": "docker",
      "appPath": "/leave-service",
      "openAPISpec": "openapi: 3.0.3\ninfo:\n  title: Leave Management API\n  version: 1.0.0\n  description: Backend API for managing employee leave requests and approvals\nservers:\n  - url: http://localhost:9090\npaths:\n  /health:\n    get:\n      summary: Health check endpoint\n      responses:\n        '200':\n          description: Service is healthy\n          content:\n            application/json:\n              schema:\n                type: object\n                properties:\n                  status:\n                    type: string\n                    example: ok\n  /api/v1/users/me:\n    get:\n      summary: Get current user profile\n      responses:\n        '200':\n          description: Current user details\n          content:\n            application/json:\n              schema:\n                $ref: '#/components/schemas/User'\n  /api/v1/leave-requests:\n    get:\n      summary: List leave requests\n      description: Employees see their own requests, managers see their team's requests\n      parameters:\n        - name: status\n          in: query\n          schema:\n            type: string\n            enum: [pending, approved, rejected]\n      responses:\n        '200':\n          description: List of leave requests\n          content:\n            application/json:\n              schema:\n                type: array\n                items:\n                  $ref: '#/components/schemas/LeaveRequest'\n    post:\n      summary: Create a new leave request\n      requestBody:\n        required: true\n        content:\n          application/json:\n            schema:\n              type: object\n              required:\n                - startDate\n                - endDate\n                - leaveType\n              properties:\n                startDate:\n                  type: string\n                  format: date\n                endDate:\n                  type: string\n                  format: date\n                leaveType:\n                  type: string\n                  enum: [annual, sick, personal]\n                reason:\n                  type: string\n      responses:\n        '201':\n          description: Leave request created\n          content:\n            application/json:\n              schema:\n                $ref: '#/components/schemas/LeaveRequest'\n        '400':\n          description: Invalid request data\n  /api/v1/leave-requests/{id}:\n    get:\n      summary: Get a specific leave request\n      parameters:\n        - name: id\n          in: path\n          required: true\n          schema:\n            type: string\n      responses:\n        '200':\n          description: Leave request details\n          content:\n            application/json:\n              schema:\n                $ref: '#/components/schemas/LeaveRequest'\n        '404':\n          description: Leave request not found\n  /api/v1/leave-requests/{id}/approve:\n    post:\n      summary: Approve a leave request (managers only)\n      parameters:\n        - name: id\n          in: path\n          required: true\n          schema:\n            type: string\n      responses:\n        '200':\n          description: Leave request approved\n          content:\n            application/json:\n              schema:\n                $ref: '#/components/schemas/LeaveRequest'\n        '403':\n          description: Not authorized to approve\n        '404':\n          description: Leave request not found\n  /api/v1/leave-requests/{id}/reject:\n    post:\n      summary: Reject a leave request (managers only)\n      parameters:\n        - name: id\n          in: path\n          required: true\n          schema:\n            type: string\n      requestBody:\n        content:\n          application/json:\n            schema:\n              type: object\n              properties:\n                reason:\n                  type: string\n      responses:\n        '200':\n          description: Leave request rejected\n          content:\n            application/json:\n              schema:\n                $ref: '#/components/schemas/LeaveRequest'\n        '403':\n          description: Not authorized to reject\n        '404':\n          description: Leave request not found\n  /api/v1/leave-balance:\n    get:\n      summary: Get current user's leave balance by type\n      responses:\n        '200':\n          description: Leave balance by type\n          content:\n            application/json:\n              schema:\n                type: object\n                properties:\n                  annual:\n                    type: number\n                    format: float\n                  sick:\n                    type: number\n                    format: float\n                  personal:\n                    type: number\n                    format: float\ncomponents:\n  schemas:\n    User:\n      type: object\n      properties:\n        id:\n          type: string\n        email:\n          type: string\n        name:\n          type: string\n        role:\n          type: string\n          enum: [employee, manager]\n        managerId:\n          type: string\n    LeaveRequest:\n      type: object\n      properties:\n        id:\n          type: string\n        employeeId:\n          type: string\n        employeeName:\n          type: string\n        startDate:\n          type: string\n          format: date\n        endDate:\n          type: string\n          format: date\n        leaveType:\n          type: string\n          enum: [annual, sick, personal]\n        reason:\n          type: string\n        status:\n          type: string\n          enum: [pending, approved, rejected]\n        createdAt:\n          type: string\n          format: date-time\n        reviewedAt:\n          type: string\n          format: date-time\n        reviewedBy:\n          type: string\n        rejectionReason:\n          type: string",
      "componentAgentInstructions": "Implement a Go backend service using net/http that listens on port 9090. Use an in-memory data store with predefined seed data for users, leave balances, and leave requests. Define User struct with id, email, name, role (employee or manager), and managerId. Define LeaveRequest struct matching the OpenAPI schema. Seed at least 3 employees and 1 manager with reporting relationships. Initialize leave balances (annual: 20 days, sick: 10 days, personal: 5 days per employee). Implement GET /health returning {\"status\":\"ok\"}. Implement GET /api/v1/users/me to return current user based on a mock user ID from header X-User-Id (default to employee1 if missing). Implement GET /api/v1/leave-requests: if user is employee, return only their requests; if manager, return requests from direct reports. Support optional query param status to filter. Implement POST /api/v1/leave-requests to create a new request with status pending, validate dates and leave type, generate unique ID, store it. Implement GET /api/v1/leave-requests/{id} to retrieve a single request if user has access. Implement POST /api/v1/leave-requests/{id}/approve: check if current user is a manager and the request belongs to their direct report, update status to approved, set reviewedAt and reviewedBy. Implement POST /api/v1/leave-requests/{id}/reject: same authorization as approve, update status to rejected, store optional rejection reason. Implement GET /api/v1/leave-balance to return current user's balance by leave type. Use JSON for all request/response bodies. Return appropriate HTTP status codes (200, 201, 400, 403, 404). Log all requests. Use standard library only, no external dependencies except for routing if needed (can use net/http ServeMux). Ensure thread-safe access to in-memory data with sync.RWMutex. Provide a Dockerfile with multi-stage build using golang:1.21 as builder and alpine as runtime. Expose port 9090."
    },
    {
      "name": "leave-web-app",
      "componentType": "web-app",
      "language": "TypeScript / React",
      "dependsOn": [
        "leave-service"
      ],
      "entrypoint": "deployment/web-application",
      "buildpack": "docker",
      "appPath": "/leave-web-app",
      "openAPISpec": "openapi: 3.0.3\ninfo:\n  title: Leave Management Web Application\n  version: 1.0.0\n  description: Frontend single-page application for leave management\nservers:\n  - url: http://localhost:3000\npaths:\n  /health:\n    get:\n      summary: Health check endpoint\n      responses:\n        '200':\n          description: Web app is healthy\n          content:\n            application/json:\n              schema:\n                type: object\n                properties:\n                  status:\n                    type: string\n                    example: ok",
      "componentAgentInstructions": "Implement a React single-page application using TypeScript, React 18, and Vite as the build tool. Create a responsive UI with the following pages: Dashboard (shows user info and leave balance), My Requests (employee view of their leave requests with status), Team Requests (manager view of pending requests from direct reports), and New Request (form to submit a leave request). Use React Router for navigation. Fetch data from leave-service at http://leave-service:9090/api/v1 (configure via environment variable VITE_API_BASE_URL). Send X-User-Id header with every API request (mock user selection dropdown in dev mode or read from a cookie). Use axios or fetch for API calls. Display leave balance as cards showing available days per leave type (annual, sick, personal). For My Requests page, show a table with columns: dates, type, status, reason, and action buttons (view details). For Team Requests page (visible only to managers), show pending requests with employee name and approve/reject buttons. New Request page should have a form with date pickers for start/end date, dropdown for leave type, textarea for reason, and submit button. Validate that end date is after start date. Show success/error toasts after actions. Use a simple CSS framework like Tailwind CSS or Material-UI for styling. Implement GET /health endpoint in the Express or static server serving the React app, returning {\"status\":\"ok\"}. Provide a Dockerfile with multi-stage build: build stage uses node:18 to run npm install and npm run build, runtime stage uses nginx:alpine to serve the built static files. Copy build output to /usr/share/nginx/html. Expose port 3000. Configure nginx to route API requests to leave-service if needed, or handle CORS in the frontend. Include environment variable substitution for API base URL at runtime."
    }
  ],
  "sourceSpec": "spec-v1"
}